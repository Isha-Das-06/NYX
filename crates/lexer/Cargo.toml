[package]
name = "nyx-lexer"
version = "0.1.0"
edition = "2021"
description = "Lexer for the Nyx programming language"
license = "MIT"

[dependencies]
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
